{application, ekub, [
    {description, "Kubernetes API client library"},
    {vsn, "0.2.0"},
    {modules, []},
    {applications, [kernel, stdlib, inets, jsx, yamerl, ewsc, hackney]},
    {env, [
        {git_sha, lists:sublist(os:cmd("git rev-parse HEAD 2>/dev/null"), 8)}
    ]}
]}.
